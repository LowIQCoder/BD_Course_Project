# SQL

In this dirrectory you will find all our `.sql` and `.hql` scripts. 

## Naming Conventions

To add some structure we decided to name our files in the following way:

```bash
stage<stage_num>_<file_num>_<description>.sql
```

So anyone can understand order of execution of this files.
